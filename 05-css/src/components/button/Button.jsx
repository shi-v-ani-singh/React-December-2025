import styles from './Button.module.css'

const Button = () => {
  return (
    <div>
        <button className= {styles.btn}>
          This is button.
          </button>
    </div>
  )
}

export default Button